import csv

row = ["Abner", "Saavedra", 32 ]

# Abre el archivo en modo de escritura
with open("archivoCSV.csv", "w") as file:
    # Crea un csv writer
    csv_writer = csv.writer(file)

    # Escribir una fila en el archivo csv
    csv_writer.writerow(row)